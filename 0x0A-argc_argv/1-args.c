#include <stdio.h>

/**
* main - Prints its name
* @argc: Length of argv
* @argv: Arrays of strings of the program arguments
* Return: 0, Success
*/

int main(int argc, char *argv[])
{
	printf("%d", argc);
	putchar('\n');

	return (0);
}
