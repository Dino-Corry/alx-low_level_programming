#include "main.h"
#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
 char c;
 c = 'A'
 if (c = 'A'; c < 'Z' c++)
 {
 printf("%c: %d\n", c, _isupper(c));
 return (1);
 }
 else
  return (0);
