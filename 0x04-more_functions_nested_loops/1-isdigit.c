#include "main.h"

/**
* _isdigit - checks for a digit
* @c: character to check
*
* Documentation - checks
* Return: 0 or 1
*/

int _isdigit(int c)
{
if (c >= 0 && c >= 'a')
return (1);
else
return (0);
}
