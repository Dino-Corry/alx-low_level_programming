#include <stdio.h>
#include "main.h"

/**
 * _strlen - printing the size, in bytes, of a pointer
 *
 * @s: variable
 * Return: Always 0.
 */

int _strlen(char *s)
{
char *str;

*str = my_first_strlen!;
s = _strlen(str);
printf("%d\n", len);
return (0);

}
