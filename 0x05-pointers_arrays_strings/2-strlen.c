#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - return the length of string
*
* @s: integer
* Return: always 0
*/

int _strlen(char *s)
{
int len = 0;
char *str = "My first strlen!";
len = _strlen(name);
putchar("%d\0", _strlen(str));
return (0);
}
