#include "main.h"
#include <stdio.h>

/**
* _strlen - return the length of string
*
* @s: integer
* Return: always 0
*/

int _strlen(char *s)
{
int len = 0;
while (str[len])
{
len++;
return (len);
}
}
