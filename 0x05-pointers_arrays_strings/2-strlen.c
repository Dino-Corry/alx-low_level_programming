#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - return the length of string
*
* @s: integer
* Return: always 0
*/

int _strlen(char *s)
char *str = "My first strlen!";
printf("%d\n", strlen(str));
return (0);
}
