#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - return the length of string
*
* @s: integer
* Return: always 0
*/

int _strlen(char *s)
{
char *str = "My first strlen!";
*s = _strlen;
putchar("%c", _strlen(str));
return (0);
}
