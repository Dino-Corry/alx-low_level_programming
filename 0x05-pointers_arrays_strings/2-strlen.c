#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strlen - return the length of string
*
* @s: integer
* Return: always 0
*/

int _strlen(char *s)
{
s = 0;
char *str = "My first strlen!";
_putchar("%d\0", strlen(str));
return (0);
}
