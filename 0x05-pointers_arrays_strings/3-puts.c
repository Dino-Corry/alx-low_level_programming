#include <stdio.h>
#include "main.h"

/**
 * _strlen - return the length of string
 *
 * @s: string to evaluate
 * Return: return the length
 */

void _puts(char *str)
{
int n;
n = 0
_put("&d\n");
  return (0);

}
