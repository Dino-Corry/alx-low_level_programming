#include <stdio.h>
#include "main.h"

/**
 * reset_to_98 - printing the size, in bytes, of a pointer
 *
 * @n: variable
 * Return: Always 0.
 */

void swap_int(int *a, int *b)
{

a = 98;
b = &a;
printf("a=%d, b=%d\n", a, b);
return (0);

}
