#include <stdio.h>
#include "main.h"

/**
 * reset_to_98 - printing the size, in bytes, of a pointer
 *
 * @n - variable
 * Return: Always 0.
 */

void reset_to_98(int *n)
{
int *n;

n = 98;

printf("%p" is the value of n\n", n);
return (0);
}
