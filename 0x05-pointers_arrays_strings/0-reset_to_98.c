#include <stdio.h>
#include "main.h"

/**
 * reset_to_98 - printing the size, in bytes, of a pointer
 *
 * @p: variable
 * Return: Always 0.
 */

void reset_to_98(int *n)
{
int *q;

q = 98;

printf("%p" is the value of q\n", q);
return (0);
}
