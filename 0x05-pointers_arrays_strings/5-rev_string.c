#include "main.h"
#include <stdio.h>

/**
 * rev_string - string in reverse
 * @s: string to be printed
 */
void rev_srting(char *s)
{
int i = 0, n, a;
while (s[i])
{
i++;
}
a = i;
for (n = a - 1; n >= 0;)
{
n--;
return (0);
}
